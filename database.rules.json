{
  "rules": {
    "banner": {
      ".read": true,
      ".write": false
    },
    "carts": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}